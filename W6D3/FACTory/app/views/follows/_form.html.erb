<% if current_user.follows?(user) %>
  <% status = 'followed' %>
<% else %>
  <% status = 'unfollowed' %>
<% end %>

<button class="follow-toggle" data-user-id="<%=user.id%>" data-follow-state="<%= status %>" type="button" name="follows"></button>
